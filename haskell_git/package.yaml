name: hgit
version: 0.1.0.0

dependencies:
  - base == 4.*
  - directory
  - filepath
  - transformers

library:
  source-dirs: src
  # dependencies:

executables:
  hgit:
    main: Main.hs
    source-dirs: app
    dependencies:
      - hgit
      - optparse-applicative
